import styles from "./LoginPage.module.scss";

const LoginPage = () => {
    return (
        <div className="container">
            <div className={styles.layout}>
                <h1 className={styles.layoutTitle}>
                    Страница входа еще не готова 😔
                </h1>
            </div>
        </div>
    );
};

export default LoginPage;
